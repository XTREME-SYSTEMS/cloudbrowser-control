{
  "name": "Webhook",
  "type": "object",
  "properties": {
    "name": {
      "type": "string"
    },
    "url": {
      "type": "string",
      "description": "URL to send webhook payloads to"
    },
    "events": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "job.completed",
          "job.failed",
          "session.created",
          "session.ended",
          "schedule.triggered",
          "change.detected",
          "engine.down",
          "budget.alert"
        ]
      }
    },
    "secret_encrypted": {
      "type": "string",
      "description": "AES-GCM encrypted HMAC signing secret (base64). Never returned to frontend."
    },
    "has_secret": {
      "type": "boolean",
      "default": false,
      "description": "Whether a signing secret is configured (safe for frontend display)"
    },
    "active": {
      "type": "boolean",
      "default": true
    },
    "last_triggered": {
      "type": "string",
      "format": "date-time"
    },
    "last_status": {
      "type": "number"
    },
    "provider": {
      "type": "string",
      "enum": [
        "generic",
        "slack",
        "discord"
      ],
      "default": "generic"
    },
    "project_id": {
      "type": "string",
      "description": "Project this webhook is bound to (V1.1 F-06 \u2014 used for inbound job/project scoping)"
    }
  },
  "required": [
    "name",
    "url"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}