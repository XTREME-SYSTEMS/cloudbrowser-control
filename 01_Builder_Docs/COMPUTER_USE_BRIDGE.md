# Computer Use Bridge

Cloud lane: Xtreme Cloud Browser MCP / Railway Playwright.
Local lane: Remote Desktop Commander on JARVIS-COMMAND.

JARVIS is a controlled mirror/executor, never the production authority. When offline, queue local work and continue cloud-safe work. On reconnect verify device identity, allowed directories, boot/lease state, then resume idempotent work.

Do not make security/network/permission/destructive local changes without explicit scoped approval.
