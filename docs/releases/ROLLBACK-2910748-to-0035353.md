# Rollback Receipt — V1.1 Fortress Incident Containment

**Receipt ID:** ROLLBACK-2910748-to-0035353
**Date:** 2026-08-20
**Type:** Incident rollback (production restoration)
**Operator / Release Approver:** @xps-admin
**Author ≠ Approver:** satisfied (builder authored evidence; approver executed Git + Railway restoration)

---

## 1. Incident (pre-rollback state)

| Field | Value |
|---|---|
| Escaped commit | `2910748fc79d652b2fde8be2cfcc02c9a045631f` (V1.1 Fortress) |
| Escape path | `fortress/v1.1` → `main` (branch boundary violation) → Railway auto-deploy |
| Engine pool | **0/3** (capacity 3) — pool warming broken by V1.1 engine code |
| Worker restarts observed | 3 distinct worker IDs over the incident window |
| Deployment version live | v6.0.0 |
| Function drift | widespread; `triggerWebhook` MISSING |
| Critical defects | 0 |
| High defects | 0 |
| Non-terminal sessions | 0 orphans |

**Root cause:** a development commit on `fortress/v1.1` reached `main` and auto-deployed to Railway production, breaking the browser-engine session pool. No branch protection, no production deploy gate, and Railway auto-deploy on `main` allowed the escape.

---

## 2. Restoration (post-rollback state)

| Field | Value |
|---|---|
| Restored baseline (main) | `0035353272cec572ea36cef12bba3aad2793db53` (pre-Fortress) |
| Restoration method | Git tree restore to pre-Fortress baseline (no force-push, no history rewrite) |
| Railway deployment | successful (replacement worker provisioned) |
| Engine status | **healthy** |
| Engine version | 3.0.0 |
| Engine pool | **3/3** (capacity 3) ✅ |
| Max sessions | 10 |
| Deployment version (post-restore) | v5.0.0 (V1.0 baseline) |
| Critical defects | **0** ✅ |
| High defects | **0** ✅ |
| Non-terminal sessions | 0 (reconciled) |
| Secrets changed | none (ENCRYPTION_KEY, ENGINE_API_KEY, ENGINE_URL, CAPTCHA_SOLVER_API_KEY untouched) |
| Data destroyed | none |
| Railway modified directly by builder | no (restoration via corrected main + Railway auto-deploy) |

**Evidence sources:** `engineHealth` (live probe), `getDeploymentStatus` (drift matrix), `ErrorPattern` entity reads (critical/high), `Session` entity reads (running orphans).

---

## 3. Rollback procedure executed

```
1. Trigger: pool 0/3 + drift > 0 + V1.1 source on main
2. Preserve Fortress source → fortress/v1.1 @ 2910748 (branch + snapshot)
3. Restore main tree to 0035353 (pre-Fortress baseline)
   - git read-tree -u --reset 0035353
   - git commit -m "restore: pre-Fortress runtime baseline"
   - git push origin main          (no force-push)
4. Railway auto-deploy from corrected main → replacement worker → pool 3/3
5. Verify: engineHealth healthy, pool 3/3, 0 critical, 0 high, 0 orphans
6. Publish this receipt
7. STOP — do not resume Fortress development
```

No force-push. No history rewrite. No secret changes. No data destruction. No direct Railway mutation by the builder.

---

## 4. Fortress source preservation

- Branch `fortress/v1.1` retained at `2910748fc79d652b2fde8be2cfcc02c9a045631f`.
- Incident documentation preserved in `docs/FORTRESS_RECEIPTS.md` and `docs/GOVERNANCE_HARDENING_PLAN.md`.
- Fortress source is **frozen** pending explicit operator authorization (`FORTRESS RESTART`).

---

## 5. Current main (post-restoration, pre-governance-hardening)

- `main` = `74f0993b231fc8f490c7fe1882ca564679a00efb` (governance-hardening commit on the restored baseline; no V1.1 runtime code).
- Engine: healthy · Pool: 3/3 · Critical: 0 · High: 0.

---

## 6. Rollback proof status

**PASS** — the incident rollback was executed and verified with live evidence. No additional rollback is manufactured; this receipt formalizes the restoration already performed.

| Check | Result |
|---|---|
| Pre-rollback pool 0/3 documented | PASS |
| Restoration SHA recorded (0035353) | PASS |
| Railway redeploy successful | PASS |
| Replacement worker provisioned | PASS |
| Post-rollback pool 3/3 | PASS |
| Post-rollback engine healthy | PASS |
| Post-rollback critical 0 / high 0 | PASS |
| No force-push / no history rewrite | PASS |
| No secret / data changes | PASS |
| Fortress source preserved | PASS |

**Rollback Receipt: PASS**